{
  "scripts": {
    "build": "npm install --prefix Frontend && npm run build --prefix Frontend",
    "start": "npm run preview --prefix Frontend -- --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {},
  "devDependencies": {}
}